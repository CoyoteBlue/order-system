import { StarterfilterPipe } from './starterfilter.pipe';

describe('StarterfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StarterfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
